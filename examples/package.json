{
  "name": "example",
  "version": "1.0.0",
  "author": "romnn <contact@romnn.com>",
  "description": "usage example for downloading github releases",
  "license": "MIT",
  "scripts": {
    "run": "rimraf ./tmp && ts-node download.ts",
    "check": "tsc --noEmit",
    "format": "prettier . --write"
  },
  "dependencies": {
    "action-get-release": "file:.."
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "prettier": "^3.5.3",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
